FREE_WARNING = 'Free shipping only applies to single customer orders';
BANNED_WARNIN = 'Unfortunately we do not ship to your country of residence';
NONE_SELECTED = '0'

if (location = RSA) {
    shipping === 400 && currency === 'R'

} else if (location = NAM) {
    shipping === $600

} else if (location = NK)
    shipping === 800 && currency === $

shoes = 300 * 1;
toys - 100 * 5;
shirts = 150 * 'NONE_SELECTED';
batteries = 35 * 2;
pens = 5 * 'NONE_SELECTED'

shipping = null
currency = $

if (shoes + batteries + pens + shirts > 1000 ) {

} else if (location = NAM && customers < 2) {

} else if (location = RSA)
    shipping = 0 || calcShipping


if (shipping >= 1000 && customers === 1) {
     +
} else if (location = RSA || NAM) {
    shipping === free

}
console.log(WARNING)

if ('price', currency, shoes + batteries + pens + shirts + shipping) {

}

const customers = '1';
const location = 'RSA';
const currency = null